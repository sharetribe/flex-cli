(ns sharetribe.flex-cli.commands.auth)

(defn login [opts]
  (println "TODO")
  (println "login command, options:" opts))

(defn logout [opts]
  (println "TODO")
  (println "logout command, options:" opts))
